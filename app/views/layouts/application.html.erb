<!DOCTYPE html>
<html>
<head>
  <title>Idea Tree</title>
  <link rel="shortcut icon" href="/assets/icon.ico" />
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container-fluid">
  <div class="header-info" style="height: 30px;">
	  <div class="login_account_detail">
		  <ul class="hmenu ">
      	  <% if user_signed_in? %>
          <a href="/users/<%= current_user.id %>" class="btn btn-primary btn-lg active" role="button">
        	Logged in as <strong><%=current_user.email%></strong></a>
      	  <% end %>
		    <%= render 'devise/menu/registration_items' %>
		    <%= render 'devise/menu/login_items' %>
		  </ul>
	  </div>
  </div>
  <div class="home_nav_bar">
    <% if current_user.present? %>
      <h1>Shape you idea with us</h1>
      <div class="container">
        <%= render :partial => "shared/home_nav_bar" %><br>
      </div>
      <br>
    <% end %>
  </div>
  <%= yield %>
</div>

</body>
</html>
