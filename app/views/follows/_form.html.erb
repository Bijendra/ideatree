<%= simple_form_for(@follow) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :obj_type %>
    <%= f.input :obj_id %>
    <%= f.input :user_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
