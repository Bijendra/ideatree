
<% p "$"*200 %>
 
<div style="display:table;">
    <table style="margin:5px;" class='table' id='comment_table'>
	  <thead>
	    <tr>
	      <th>Comments</th>
	    </tr>
	  </thead>
	  <%= link_to 'More Comments', comments_path(assignment),:locals => {:comments => @comments[assignment], :com => @com, :assignment => assignment} %>
	  <tbody id="comment-modal">
	  	    <% comments.last(2).each do |comment| %>
		<tr><td><%= comment.content %></td></tr>	
		<% end -%>	
		</tbody>	
		</table>	
		</div>	



<div style="display:table;">
    <table style="margin:5px;" class='table' id='comment_table'>
	  <thead>
	    <tr>
	      <th>Content</th>
	    </tr>
	  </thead>
	  <tbody id="comment-modal">
	    <% comments.each do |comment| %>
	      
	         <%= render partial: 'comments/table_row', locals: {comment: comment, assignment_id: assignid} %>

	        <%#= "comment assignment id : #{@assignment.id}" %>
	        <!--<td><%= comment.content %></td>
	        <%# if current_user.id.to_s == comment.user_id.to_s %>
	        <td><%#= link_to 'Like', edit_comment_path(comment) %></td>

	        <td><%#= link_to 'Edit', edit_comment_path(comment) %></td>
	        <td><%#= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	        <%# end %> -->
	       
	    <% end %>
	  </tbody>
	</table>
</div>

<%= form_for(com, remote: true, url: create_comment_ajax_url) do |f| %>

	<div class="field" >
	  	<%= f.label :comment_here %>:
  		<%= f.text_field :content, :id => "com" %><br />

  	</div>
  	<div class="actions">
		<%= submit_tag onSubmit="submitComment();"%>
		    <%#= hidden_field_tag 'assignment_id', assign %> 
		   <script type="text/javascript">
		   		$('#com').val('');
		   </script>
	</div>
<% end %>


<script>
  var submitComment = function(){
      $.ajax(
      	url: "/create_comment_ajax",
      	method: post,
      	//data: 1,
    	data: {content: content, assignment: assign},
		success: function displayBackComment(){
           $("#comment-modal").append("content");
           $("#comment-modal").modal("content");

    	 }
     
  	 )};
</script>

