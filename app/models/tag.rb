class Tag < ActiveRecord::Base

# this will be more like attach with the project to better search capabilities
# this will be a table which will keep on creating tags.use tagit to bring tags like stackoverflow
end
